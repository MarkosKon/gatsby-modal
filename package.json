{
  "name": "gatsby-modal",
  "private": true,
  "description": "Gatsby modal",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write src/**/*.{js,jsx}",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "netlify deploy --dir=public",
    "deploy:prod": "netlify deploy --dir=public --prod",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\""
  },
  "dependencies": {
    "@affectionatedoor/gatsby-theme-ui": "^3.0.0",
    "gatsby": "^4.25.7",
    "gatsby-source-instagram": "^0.5.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-modal": "^3.9.1"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
